# llm-honeypot

An SSH honeypot that accepts any login and hands the attacker a convincing, LLM-driven
fake Linux box. It logs everything they try, keeps them busy, and produces a monthly
"what the bots did on my server" report.

## What it is

- A Go SSH server that accepts any username/password (after a plausible delay) and
  presents an interactive shell.
- The shell is fake: a consistent virtual filesystem, believable command output,
  fake users, fake cron jobs, fake credentials that lead nowhere. Common commands are
  emulated in code; unknown commands and file contents are generated by a language
  model, constrained to stay consistent with the virtual machine's state.
- Every session is recorded: credentials tried, commands, downloaded payload URLs,
  timing, source ASN/country. Sessions are clustered into attacker behaviours.
- Reports: top credentials, top first commands, new payload families this month, how
  long attackers stayed, the funniest transcripts.

## Why build it

It is mischief with real-world data. Real bots will show up within hours of exposing
port 22 on any VPS, and watching them fumble through a shell that is politely lying to
them is a great way to learn what automated attacks actually do. It is also a serious
exercise in prompt-injection resistance, since every attacker input is untrusted and
some of it will be aimed at the model.

## How it works (planned)

```
attacker ──ssh──► Go SSH server (accept-all auth, per-session sandbox state)
                    │
                    ├── emulated commands (ls, cd, cat, ps, uname, wget/curl → logged, …)
                    │      backed by a virtual filesystem seeded per "machine profile"
                    │
                    ├── fallback: model generates output for unknown commands / files,
                    │      given the machine profile + state; output is validated and
                    │      written back to the virtual FS so later commands agree with it
                    │
                    └── recorder: full transcript, timings, extracted URLs/hashes
                                     ▼
                       clustering + monthly report + dashboard
```

- **Consistency over cleverness.** The virtual machine has one state; anything the
  model invents becomes part of that state so `cat` twice returns the same file.
- **Never execute anything.** No real shell, no real network; downloads are logged, not
  fetched (optionally fetched later in an isolated sandbox for analysis).
- **Prompt-injection hardening.** Attacker text is data. The model sees it inside a
  fixed frame and can only produce shell output; anything else is dropped.
- **Cheap by design.** Emulated commands cover the long tail; the model is called only
  for novel commands, with aggressive caching of the machine profile.

## What gets measured

- Sessions per day, median dwell time, commands per session, model calls per session.
- Detection of "honeypot suspected" behaviour (attackers leaving quickly after a probe).
- Monthly report published from the dashboard.

## Deployment notes

Runs on a cheap VPS in a container with no outbound network except the model API and
the log sink. Real SSH on a different port with key-only auth. Never on a machine that
matters.

## Planned stack

Go (`golang.org/x/crypto/ssh`), SQLite or Postgres for sessions, a small dashboard,
GeoIP/ASN lookup, model access behind one interface.

## Milestones

- **v0** — accept-all SSH server, emulated core commands, transcript logging.
- **v1** — virtual filesystem + model fallback with state write-back; deploy on a VPS.
- **v2** — clustering, dashboard, first monthly report.
- **v3** — payload capture into an isolated sandbox; honeypot-detection evasion study.

## Status

Under construction on branch `feat/v0`: the SSH layer (accept-all auth, sessions, host key),
config, the machine profile and the virtual filesystem exist and are tested. The fake shell,
recorder and model fallback are next — see `CLAUDE.md` for the ordered plan.

```sh
make build && ./bin/honeypot -addr :2222 -data ./data   # currently answers with a placeholder
make test && make lint
```
